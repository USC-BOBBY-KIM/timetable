{
  "name": "campus-week-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check scripts/generate-usc-data.mjs"
  },
  "engines": {
    "node": ">=20 <25"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  }
}
